'use strict';
// Optional server endpoint only. Never place an OpenAI API key or reusable secret here.
// Example: https://your-domain.example/run-realms/blueprint
window.RUN_REALMS_CONFIG=Object.freeze({
  aiBlueprintEndpoint:'',
  aiTimeoutMs:1800,
  build:'1.0.0'
});
